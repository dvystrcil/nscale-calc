apiVersion: v1
kind: Service
metadata:
  name: nscale-calc
  namespace: nscale-calc
  labels:
    app: nscale-calc
spec:
  selector:
    app: nscale-calc
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
